import { Plugin } from 'vite';

export declare type Options = {
    robotsDir?: string;
    outputRobotsFileName?: string;
    enableDebug?: boolean;
};

export declare function robots(userOptions?: Options): Plugin;

export { }
